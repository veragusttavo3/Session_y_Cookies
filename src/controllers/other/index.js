//esto es un objeto literal de funciones (metodos), que se va a redigir a rutas, tmb mediantes requiere

module.exports = {
formulario: require('./formulario.controller'),
proceso: require("./proceso.controller")

}